version: '3.8'

services:
  app:
    build:
      context: .
      target: release
    container_name: auth-service
    env_file:
      - ./.env.prod
    ports:
      - '3001:3001'
    networks:
      - ibahbalezin-net

networks:
  ibahbalezin-net:
    external: true
